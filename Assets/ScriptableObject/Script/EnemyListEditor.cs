using UnityEditor;
using UnityEngine;

#if UNITY_EDITOR
//[CustomEditor(typeof(EnemyList))]
/*public class EnemyListEditor : Editor
{
    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        EnemyList ts = (EnemyList)target;

        if (GUILayout.Button("REFRESH"))
        {
            ts.RefreshList();
        }
    }
}*/
#endif
